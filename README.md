CrossTalk Data Platform
=======================

To build this app you'll need to have the Play framework installed. 
To build, simply use the ```activator``` command, e.g., 


```activator compile```

```activator run```

```activator clean```
